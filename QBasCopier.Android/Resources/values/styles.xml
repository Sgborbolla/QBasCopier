<resources>
  <!-- AvaloniaMainActivity deriva de AppCompatActivity: el tema DEBE ser descendiente de Theme.AppCompat,
       si no revienta con "You need to use a Theme.AppCompat theme (or descendant) with this activity". -->
  <style name="MyTheme" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowBackground">@android:color/black</item>
  </style>
</resources>